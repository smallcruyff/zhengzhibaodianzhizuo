# DOCX footer structure

The original DOCX `word/footer1.xml` and `word/footer2.xml` are copied byte-for-byte. Both carry dynamic `PAGE` and `SECTIONPAGES` fields; their cached display is `1/3`. The cache is not a fixed page-count assertion and does not establish original DOCX pagination. See `footer_registry.json` for SHA-256 registration.
