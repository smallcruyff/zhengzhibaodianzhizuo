# gpt和claude共同的小窝：当前协作入口（2026-09-14）

## 2026-09-22 模型要求覆盖

用户明确要求采用 GPT-6 Luna 和 GPT-6 Sol，不再使用 5.6 系列。Codex 新派辅助任务默认 GPT-6 Luna/max；主代理保持用户当前所选模型，指定 Sol 时使用 GPT-6 Sol。尚未完成的 5.6 子任务停止后接续到新模型，已完成的有效结果和历史调用记录照实保留。此项覆盖 Skill 平台表中的旧 gpt-5.6-luna 指定。

用户已授权共同资料、规则与交接配置，最终选用Claude桌面软件里的Code本地模式。每次开工先读同目录[00_协作入口.md](00_协作入口.md)。实际项目目录已改为`/Users/wanglifei/Desktop/gpt和claude共同的小窝`；旧`/Users/wanglifei/Desktop/gpt6`是隐藏兼容链接，指向同一批文件，新成果优先使用新路径。原始题源未移动。

完整规则现为Skill 6.3.0，执行性Luna/max要求按其AGENTS中的Codex/Claude平台表适配；历史调用和记录保持原样。共用当前稿与进度，每册单一合稿负责人；每批写入前核`协作/接管.json`与真实父稿。配置不转交必修二或必修三、不启动正文、不改现有审阅稿。当前具体册别与新授权仍由用户本轮任务决定。

启动及明确规则/题源变化后运行`python3 Claude协作同步_20260914/collab.py sync`；每批接续运行status并读取实际当前进度。新要求及时写入本册既有记录，交接使用checkpoint/offer/accept，不只留在聊天中。详见共同入口。

以下为保留的历史要求，具体版本号与分工以本册实际最新记录及后续用户要求核定。

# 北京高考政治宝典最后一站 GPT6

用户要求：发出指令后，对不能从现有资料核实、且会实质影响结果的不确定事项先询问；已明确的要求不重复确认。

## 成果存放位置（2026-09-08最新要求）

本项目主文件夹已经由用户改为 `/Users/wanglifei/Desktop/gpt6`，且已从应用的项目登记中核实。此后新生成的宝典 Word/PDF、审计表、压缩包、样稿、核验结果和工作脚本，均存入本目录下各宝典自己的子目录。**不要再把新成果写入“2026模拟题”等题源目录。** 活跃任务的进程工作目录可能仍是旧目录，不能因此沿用旧输出地址；调用旧脚本前先检查并调整输出路径。

必修二已于2026-09-13由用户确认正文制作收口，接受版本为修订214；当前身份见`必修二_周六成品冲刺_20260910/状态/当前基线.json`，人工入口见下文。封面和前言由用户手工完成，不自行继续改正文。`必修二_v25续作_20260908/00_当前工作状态.md`保留为历史定位入口。其他册沿用各自真实版本链，不互相覆盖。

原始资料和历史成果保留原位，本次要求不授权删除、移动原件或批量搬迁全部历史文件。以下目录继续作为只读题源与正式细则来源：

- `/Users/wanglifei/Desktop/2024模拟题`
- `/Users/wanglifei/Desktop/2025模拟题`
- `/Users/wanglifei/Desktop/2026模拟题`
- `/Users/wanglifei/Desktop/历年高考题及细则`

## 必修二每轮人工审查入口（2026-09-10用户新增）

每轮完成、当前工作头与对应原生PDF确认后，更新 `/Users/wanglifei/Desktop/gpt6/00_必修二最新审查稿`。该目录顶层只展示最新一份Word、一份PDF和“先看这里.md”；文件名带修订号，说明列本轮变化、更新时间和审查状态。未完成全书终审时明确称“阶段审查稿”，不冒称成品。不要再让用户从工程目录里的多个中间Word自行判断最新版。

由必修二中央总控在每轮落地后调用 `必修二_周六成品冲刺_20260910/规划审查/审查入口/publish_review.py`，使用本机文档Python运行，以 `--expected-revision` 指定当前修订，并用重复的 `--change` 参数提供本轮简短变化说明。脚本只读取“状态/当前基线.json”中已确认的工作头，验证Word/PDF指纹与页数；不会将未晋升候选稿误发为最新完成稿，也不会改动书稿或中央状态。每轮更新审查入口是该轮交付步骤，不能只在消息中口头称已更新。

上一轮人工审查副本（包括可能新增的用户批注）移到 `/Users/wanglifei/Desktop/gpt6/必修二_人工审查历史`；所有工程原件保留。禁止清空目录或删除未登记用户文件。给用户提供固定审查目录与当前文件的明确链接，并以“修订号＋PDF页码”接收修改意见。

### 2026-09-10最新：审阅入口与工程区分开

用户再次要求明确最新版位置、整理文件夹，工程文件不要干扰人工审查。固定审阅入口仍为 `00_必修二最新审查稿`，不另开多个“最新”目录。若已经保存的最新候选尚未完成原生PDF导出，可先向该入口复制一份明确标注“最新工作稿、PDF待同步、非终审”的候选快照；不得把它说成已晋升工作头，也不得搭配旧PDF冒充同版。目录只留一份最新整本Word；有真正匹配的PDF时再放入。独立题肢文件须同版，单放该入口的“题肢单练”子目录；未生成新版本时不放旧版充数。

工程和历史统一通过 `99_工程与历史归档` 进入。进行中工程、前期资料、历史审阅目录保持原绝对路径，已从Finder日常主目录视图收起，并在99目录提供入口，避免打断活跃任务；后续代理不得重建这些旧位置或误判为丢失。闲置迁移包、已批准的旧小样、零散工程文件已实移到99目录；原始题源和用户历史批注未删除。整理与可恢复路径记录保存在 `必修二_周六成品冲刺_20260910/规划审查/第三十批_审阅入口整理/整理操作与恢复记录.json`。

中央总控继续负责每批审阅入口更新。Max临时整理入口时须避免同时发布；候选快照的 `.review-manifest.json` 继续使用同一追踪格式，PDF字段为空。正式同版发布后旧快照（包括可能的新批注）照常进入历史，不覆盖用户未登记文件。

## 规则和历史入口

使用 `/Users/wanglifei/.codex/skills/beijing-gaokao-politics/SKILL.md` 当前适用规则与用户最新确认。细则说明按已认可样题精简、保留正式原话和真实分值；主代理负责核心教研与验收，明确指定 Luna/max 承担合适的重复工作。存放位置变化不改变各册结构、题源、评分证据或质量要求。

项目迁移资料仍可从 `/Users/wanglifei/Desktop/2026模拟题/项目迁移_20260908/00_当前项目迁移状态.md` 读取，仅作历史定位，不覆盖各册后续实际工作稿。旧包中的启动消息、流程与快照不独立授权正文修改、发送或发布。飞哥政治庄园属于旧体系，不以其旧流程驱动本项目。

## 2026-09-13：必修二复盘与下一本接续

已安装`beijing-gaokao-politics` Skill6.2.0。全过程要求、覆盖关系、返工教训和新任务说明位于`必修二_全程复盘与跨册工作流_20260913/00_先看这里.md`。主代理亲自负责Skill、工作流、核心教研和验收；Luna/max承担边界清楚的重复工作。旧样式按后续同范围要求覆盖，不复活方法索引等已否决结构。

用户准备另开任务继续必修三。本项目不自动创建或启动新任务；接续读取已安装Skill的必修三活动入口及复盘目录`03_必修三新任务启动说明.md`，核对实际本地R31续修稿，不退回旧快照，也不套用必修二54321或经济栏目。下一本新成果继续存gpt6下本册子目录。

新增只读题源为`/Users/wanglifei/Desktop/2023模拟题`。其中只有答案、没有正式细则的卷只处理选择题。届别以原卷核定，学生来源标题保持全书一致，不额外加印日期或届别。

必修二原Word/PDF保持修订214字节，用户接受正文与历史技术审计结论分别记录。以后用户已手改封面前言时，先核最新实物，不能用工程旧副本覆盖。本轮仅更新接受记录及入口说明，未重新发布书稿版本；下次新授权书稿修订仍执行既有审查入口流程。

## 2026-09-13：CoS仅作额度不足时的备用入口

用户明确：平常尽量由Codex直接制书；Chat On Steroids＋网页版Pro保留为额度不足时的备用，不自动切换或开启Goal/Loop。切换通过本册既有当前状态、实际工作头与SHA、已合批次及未决交接，不依赖聊天记忆互通。备用说明见 `必修三_CoS试验_20260913/02_Codex与CoS备用切换说明.md`。CoS题源共享已加入桌面2024、2025、2026模拟题，保持全局只读；新增共享不代表改变教研验收责任或授权并发写同一成品。

用户进一步要求CoS具有本项目资料，已共享2023—2026模拟题、历年高考题及细则、宝典制作原料、gpt6工程、本项目技能及旧宝典工程目录，均只读。当前映射见 必修三_CoS试验_20260913/03_当前共享目录与网页启动入口.md。默认仍Codex制作，额度不足再切换。

## 2026-09-13后续覆盖：当前阶段由网页版制作必修三

用户最新明确：“算了，目前全部让网页版来做”，并先索取当前必修三工作稿以便针对性提意见。当前阶段的必修三制作由网页版 Astra Pro 主导，覆盖上述“Codex为主、CoS备用”的旧默认；不据此推定其他册永久改变分工。主代理亲自阅读宪法、原题和正式细则，负责核心教研、争议与最终验收的质量责任可以由网页版主代理履行，不要求 Codex 将其已可靠完成的教研重新做一遍。当前 CoS 只读能力保持，文件写入、Word/PDF处理及进度保存如仍需本地执行，按明确任务交接；本指令不自行开启 Goal/Loop、不扩大工具权限、不启动并行写稿。

当前原样审阅副本见 `00_必修三最新审查稿/先看这里.md`；未修改正文，PDF同版关系尚待核定。用户批注优先保留，后续不能直接覆盖审阅副本。历史备用说明仅保留技术交接参考，任务分工以本段及用户后续指令为准。

## 2026-09-13再次覆盖：Codex修订必修三并迁新任务

用户最新要求Codex按最新版Skill实际修改必修三当前稿，多用Luna/max，核心教研由主代理把关，完成一轮后供其人工审阅。这覆盖上一段当前阶段全部交网页版的分工；CoS能力讨论和试验暂不扩展。必修二正在由用户的Chat Astra Pro审核且送印优先，其他代理不改必修二，并为其保留额度。用户随后要求新开任务，接续记录为 `必修三_最新Skill修订_20260913/00_新任务交接.md`，先收原任务三项已运行Luna检查的文件结果，不重新派重复任务。

## 2026-09-21 后勤与目录导航

本后勤任务负责共同目录整理、路径兼容及试卷/细则 Markdown 预制的质量与进度核查，不据此接管任何书稿。新分类入口见同目录 `00_目录导航.md`。所有旧工程、脚本与题库保持原绝对路径，Finder 顶层仅收起显示，分类目录以相对链接访问；不得因隐藏而重建旧目录。旧 gpt6 链接继续兼容。目录恢复、盘点和 MD 续作任务书见 `后勤管理/20260921_目录整理与MD盘点`。

用户本轮选定：先用剩余 DeepSeek 额度修复，Codex 写好续作提示词，Luna/max 做抽验。保留已有题库，只续修缺口；已经核查且对象未变的成果复用既有审计，不重复全库转换或检查。约一天的任务书已保存；是否已启动以真实任务状态为准，提示词不代表已经运行。当前不启动 Luna 全天目标任务。
