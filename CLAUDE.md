# 宝典云端仓库（gpt和claude共同的小窝 · 云端精简镜像）

这是北京高考政治宝典本地项目 `/Users/wanglifei/Desktop/gpt和claude共同的小窝` 的云端精简镜像，供用户偶尔在 Claude Code 云端会话中工作。**本地项目是唯一真源**；本仓库由本地 `Claude协作同步_20260914/云端同步.py export` 生成，不在云端反向改写镜像内容。

## 启动

1. 先读 `云端/同步清单.json`：导出时间、必修三工作头及其 SHA、当前合稿负责人、因超过 GitHub 单文件上限而只留在本地的文件。
2. 再读 `00_协作入口.md`、`.claude/skills/beijing-gaokao-politics/SKILL.md` 和其 `references/project-constitution.md`，按其中规则工作。入口里的 `/Users/wanglifei/...` 绝对路径在云端不存在：项目内路径按本仓库相对路径读；`当前状态.json` 读 `云端/必修三当前状态.json`。
3. 云端**不运行** `collab.py sync / checkpoint / offer / accept`，也不修改 `协作/接管.json`。技能母版不在云端，不在云端修改 Skill；规则建议写进候选说明，由本地主代理核定。
4. 如需渲染：环境 setup 已装 Noto CJK/文鼎楷体、LibreOffice、python-docx。云端字体与本机宋体/楷体不同，分页只作粗查，定稿渲染与页码一律回本地做，交付时注明“云端渲染引擎”。

## 云端只写候选

- 云端会话身份记为 `claude-cloud:会话ID`，始终是非负责人，按入口的候选规则工作。
- 所有产出只写 `必修三_最新Skill修订_20260913/协作/候选/Claude云端/<任务名_YYYYMMDD>/`，含候选 docx、修改说明、依据原页、未决和下一步。不改共享工作头、`00_必修三最新审查稿`、原材料和规则。
- 在会话分支上提交并推送即可，不合并到 main。用户回本地后运行 `云端同步.py pull`，文件只新增到本地同一路径，不覆盖已有文件，再由本地负责人按候选规则审定并入。

## 其他

必修二已完工送印，不得处理。题源原件、用户手改书稿与批注保持完整。用户分工、调性红线、官方原话等规则以 Skill 为准。
